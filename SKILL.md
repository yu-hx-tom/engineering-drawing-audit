---
name: engineering-drawing-audit
description: 审核客户工程图纸与 SolidWorks/CAD 重绘图的尺寸、公差、符号、视图、技术要求差异,生成审核报告。适用于扫描件、手写、旋转、多语言、低清晰度 PDF。当用户要求对比两份图纸的尺寸/公差/符号/指引线/剖面视图/放大细节/合并视图/技术要求/材料/制造备注,或要求生成核实的差异报告/标注审阅件时使用。
---

# Engineering Drawing Audit

以物理特征对应关系审核两份图纸。客户图纸作为需求来源,重绘图仅在手写不清时作为佐证。

## 准备图纸

**本环境说明**:直接使用本机 `py`(Python 3.13)与已装好的 Visuals-Skill,不涉及 Codex 的 `load_workspace_dependencies`,也没有独立的 "PDF skill"。

1. 保留两份源文件不变。
2. 检查页数、页面边框、旋转、可提取文本、扫描质量。
3. **整体解析/文档文字层**用 Visuals-Skill 的 MinerU(对 PDF/Word/PPT/Excel 提取 Markdown 文字层, 用于标题栏/技术要求检索):
   ```bash
   py 本仓库 scripts/ 的 MinerU/OCR 封装 --image "<文档.pdf>" --format json
   ```
   **注意**: 图纸的**图片读取一律走下面的 `glm_drawing.py` 封装**, 不要用 `image_to_text.py` 对图纸图片做 GLM 读取——它的 GLM 分支会把输出强制归一化到 `[TYPE]/[SCENE]/[COLOR]` 等 10 个通用标签, 破坏图纸的"视图 | 标注 | 物理特征"格式输出(2026-08-12 确认)。
   **工程图纸专用 GLM 读取(强制, 2026-08-11 优化, 2026-08-12 封装)**: 对图纸识别(尺寸/公差/符号/物理特征), 必须用本 skill 的封装 `glm_drawing.py`——它内部强制加载 `prompts/engineering-drawing.txt` 专用提示词, 提示词文件缺失时报错退出, **绝不回退到默认通用 10 维描述**:
   ```bash
   py <SKILL_DIR>/scripts/glm_drawing.py "<渲染图.png>"
   # 需要追加补充指令时(如原图反向查证找某个值):
   py <SKILL_DIR>/scripts/glm_drawing.py "<渲染图.png>" --extra "找 1705.7 附近直径并确认其数值"
   # 覆盖提示词文件 / 输出 JSON:
   py <SKILL_DIR>/scripts/glm_drawing.py "<渲染图.png>" --prompt <文件> --json
   ```
   该提示词要求: 按视图分组、物理特征具体名称(依据尺寸界线/引出线)、捕获Ø/R/±/度分秒/参考尺寸(REF/*/括号)/基准符号/形位公差框/Ra粗糙度/数量。**这是从答案级报告提炼的关键改进**——通用提示词会漏基准、形位公差、参考弧长, 且物理特征描述泛泛。
4. **高清渲染与局部放大**用本 skill 自带脚本(本地 PyMuPDF,无 API 费用):
   ```bash
   # 渲染整页为 PNG(默认 300 DPI;模糊/旋转/手写区域用 600+)
   py "${CLAUDE_SKILL_DIR}/scripts/render_pdf.py" "<输入.pdf>" "<输出目录>" --dpi 300
   # 裁剪某页局部区域并放大(如手写尺寸、放大细节)
   py "${CLAUDE_SKILL_DIR}/scripts/render_pdf.py" "<输入.pdf>" "<输出目录>" --crop <页码> <x> <y> <w> <h> --dpi 600
   ```
5. 对裁剪/渲染出的局部 PNG,再走 `glm_drawing.py` 视觉确认(复用步骤 3 的封装命令,把文件换成裁剪/渲染图)。
6. **裁剪图 GLM 识别前预处理(强制, 2026-08-12 round4 用户反馈)**:
   - **先放大再截取以保持分辨率**: 对 PDF/矢量源, `render_pdf.py --crop --dpi 600/800` 内部是 `get_pixmap(matrix=Matrix(dpi/72), clip=Rect)`——按高 DPI 渲染页面后截取区域, 即"先放大渲染再截取", 分辨率保持, 正确。对 **tif/jpg 扫描源**(物理密度仅 200-300dpi, 如 780672=200dpi、780673/780674=300dpi), 局部小标注(粗糙度/公差/角分)像素少, 需转 PDF 用高 DPI `--crop`, 或对裁剪图插值放大后再 GLM 识别。
   - **对比度增强**: 低对比度/低清扫描件(浅色标注、手写、反光、200dpi 扫描)GLM 识别前先用 `enhance_image.py` 做放大 + 对比度增强(自动对比度/CLAHE/直方图均衡), 可显著提升识别、减少漏读误读。
   ```bash
   # 裁剪图 GLM 识别前预处理: CLAHE 局部直方图均衡 + 插值放大到目标宽度
   py "${CLAUDE_SKILL_DIR}/scripts/enhance_image.py" "<裁剪图.png>" --method clahe --gray --target-width 1600 --out "<增强图.png>"
   # 再对增强图走 glm_drawing.py
   py "${CLAUDE_SKILL_DIR}/scripts/glm_drawing.py" "<增强图.png>"
   ```
   - 实测(780672, 200dpi 扫描 140x70pt 区域): 增强前 GLM 只识别 92°/30′ 两个标注; CLAHE+放大到 1600px 后识别出 30°/92°/50/74° 四个标注(50 与 74° 为增强后新识别), 印证对低清扫描件预处理的价值。
6. 矢量 PDF 先提取文本,但用渲染页面核验版式、符号、指引线、公差、上下标;扫描件先看原始分辨率。
7. 列出两份图纸的所有主视图、剖视图、放大细节、相关技术要求块。

当重绘图有可提取文本层时,尽早提取,把其尺寸、公差、符号、备注作为客户图纸的候选查找清单。此清单只是检索索引:每个候选都必须在客户源图和同一物理特征上核验,绝不单独用重绘文本判定不清晰的客户数值。

### 坐标与视图不可跨图映射(铁律)

**原图与重绘图的坐标几乎完全对不上**。原图是扫描件(可能旋转、缩放、偏移),重绘是重新布局的 CAD;即便同一特征,两边视图排布方式也可能不同;原图有的视图重绘图可能没有,或合并进其他视图。

- **禁止**把重绘文字层的尺寸坐标(x,y)用于裁剪原图识别,更不要在坐标对不上时反复试错裁剪。
- 对原图的识别,应基于**原图自身的布局**整体/分块扫描定位,而非重绘坐标。
- 特征对应只能建立在**物理特征**上(指引线/延伸线/尺寸线形状),禁止仅因坐标邻近或数值相同就视为对应(见"建立特征对应")。

### 重绘文字层 = 尺寸信息表(唯一权威),视觉只做特征归属

**核心逻辑闭环(2026-08-10 用户确立)**:
1. 读取重绘图文字层 token → 按**字号+坐标**聚合: 大字号数字=独立尺寸主值, 小字号(Φ/±/数字)=公差上标, 坐标归属到主值。
2. 结果 = 完整的**尺寸信息表**(尺寸值+公差+位置+类型), 完全由文字层机器生成, 不依赖 GLM 数值识别。
3. **尺寸信息表是唯一权威基准**。公差分离 token 用坐标聚合归属(如 Φ6→1730, +6/-3→686), 独立小数值(如 7.5)与公差(如 Φ7.5)天然区分。
4. GLM 读重绘 / GLM 读原图, 都作为**候选读数**, 拿去和尺寸信息表**核销**(主值匹配 + 公差 token 匹配)。
5. **原图反向查证 = 用重绘尺寸表去原图逐项核销**(强制, 2026-08-10 用户明确):
   - 查证**对象**是**原图**, 查证**基准**是**重绘尺寸表**(文字层权威)。
   - 先让 GLM 对原图**自由读**一遍, 得到候选读数;
   - 但**核销必须用重绘尺寸表去原图查证**: 对重绘表的**每一个尺寸**, 都在**原图**上主动确认它是否存在、数值是否一致——**不能只依赖 GLM 自由读的结果**。
   - 原因: GLM 自由读原图会**漏读非标准标注**(剖视基准直径如 1706.3、参考尺寸、粗糙度符号), 只靠自由读会把"原图有但 GLM 漏读"误判成"重绘独有"。
   - 对重绘表每个值, 若 GLM 自由读未覆盖, 必须**专门提示 GLM 去原图找该值**(如"找 1705.7 附近直径"), 确认是"原图有(数值可能不同)"还是"原图确实无"。
   - **任何"原图无X/重绘独有X"的结论, 必须先用重绘表的X去原图专门查证, 不能凭 GLM 自由读的缺失断言。**

**强制流程(脚本关卡)**:

1. **`build_dim_table.py <重绘图.pdf> <输出目录>`**
   - 产出 `dim-table.json`(尺寸信息表: 主值+公差+tol_tokens+位置) 与 `dim-table.md`(总表)。
   - 由文字层 token + 坐标聚合, 机器生成, 一个不漏。
   - **公差拼接的坐标配对规则**: 公差符号(+/±/-)与数字是分离 token, 且布局有两种——
     - 左右相邻(如 `+` x468 + `6` x472 → `+6`);
     - 同 x 上下排(如 `+` x1121 + `6` x1121 → `+6`)。
     因此每个符号 token 必须找 **x,y 最近邻的数字 token** 配对成偏差项(`+6`、`-3`), 再按 +项在前、-项在后拼接, 得到如 `686+6-3`、`115+6-3`。
   - **所有坐标匹配统一用欧几里得直线距离** `√(Δx² + Δy²)`(2026-08-11): 符号-数字配对、公差归属主值、数量前缀组合, 三处都用直线距离 + 合理阈值(如 15/45/60)。**禁止用曼哈顿距离(abs+abs)或加权和(yc*2+dx)**, 否则 y 接近时会把远距离 x 的 token 误配(如 `±5'`(x717) 误配给 `50`(x208), 实际应归属紧邻的 `74°10'`)。
   - **分组必须检查 x 连续性(用 abs 绝对值)**: 小 token 分组时 `dx < 15` 必须写成 `abs(dx) < 15`, 否则会把左侧远距离 token 收进同组, 导致公差误配(如 `±5'`(x717) 被误配给 `50`(x208), 实际应归属紧邻的 `74°10'`)。
   - **数量前缀组合**: `N-`(如 `2-`) 是数量前缀, 与相邻大字号数字(孔径)组合成 `2-∅40`(N个直径X的孔), 不能拆成独立条目。
   - 输出规范: 主值在前, 公差在后, 如 `686+6-3`(非 `6866+3-`); Φ 公差如 `1730±6`、`1070±7.5`。
   - **算法结果必须视觉核验(2026-08-11 用户强调)**: 算法(坐标聚合)处理常规布局可靠, 但边界情况可能失效(公差归属、数量组合、特殊标注)。因此生成尺寸表后, **必须用 GLM 视觉核验关键/可疑项**——对公差归属、数量组合(`2-∅40`)、角度分秒等算法易错处, 裁剪重绘对应区域让 GLM 确认读法。**算法与视觉核验结合, 不是只信算法。**
2. **`check_completeness.py <dim-table.json> <GLM尺寸清单>`**(核销, 强制)
   - GLM 读数逐条核销进尺寸信息表; 匹配上的消耗, 未匹配的报出(GLM 漏读/误读)。
   - 输出非空 → 必须逐项让 GLM 补特征归属后, 才可投入报告。
3. **报告中的"重绘侧"数据只引用尺寸信息表**, 禁止手工转写。
4. **写"重绘无某尺寸"断言前, 必须"三重确认"**: ① 尺寸信息表/台账搜索 NOT FOUND; ② 重绘视图 GLM 视觉读未捕获; ③ **定向裁剪重绘对应区域 GLM 精读确认确无**。三重缺一不可。
   - 教训(860404, 2026-08-14): 客户 `2×Ø5/8` 两孔, 重绘台账漏(Ø 矢量图形 + "2-"数量前缀 + 英制分数拆分), 重绘视图 GLM 自由读也未识别 → 仅查①+②误判 Confirmed omission; 经③定向裁剪重绘确认重绘有 `2-Ø5/8″` → Match。**台账和自由读都可能漏"数量+直径"组合(2-Ø5/8、6×Ø1 1/8 等), "重绘无 X"必须裁剪重绘核验后才有资格写**。

教训: 手工转写漏 267、1450、第二个 385/R9; 凭视觉印象把"重绘有 37 3/16"误判为"重绘无"; GLM 漏读 47°39'、160、1256、独立 7.5 而未被发现; **原图 GLM 自由读漏读 1706.3(剖视基准直径), 只靠自由读把"原图有 1706.3 vs 重绘 1705.7"误判成"重绘独有"**。这些错误在 build_dim_table + check_completeness 核销关卡下不可能发生——尺寸信息表由文字层坐标机器生成, 报告数据源被固定为脚本产物, 原图核销必须用重绘表逐项反向查证。

### 重绘图信息锚点:两个脚本流程(round5 固化, 2026-08-13)

重绘图侧的"尺寸-物理特征-视图"综合信息, 优先用两个脚本机器化获取, GLM 只做物理特征标定:

```text
重绘图 PDF
  -> extract_dimension_ledger.py -> dimension-ledger.json  (尺寸台账: 值/公差/类型/箭头引线几何归属/needs_review)
  -> detect_drawing_views.py     -> view-regions.json + crops/P1-Vxx.png  (自动视图划分, 每视图独立透明PNG)
  -> GLM 逐视图读 crops          -> 标定每个尺寸的物理特征
  = 重绘图 尺寸-物理特征-视图 综合信息
```

- 脚本位置: `仓库 scripts/ 目录下 extract_dimension_ledger.py`、`detect_drawing_views.py`。依赖 pymupdf/numpy/pillow(本机已装); `extract_dimension_ledger.py --self-test` 可自检。
- `extract_dimension_ledger.py`: 提取尺寸文字/公差/英制分数/类型, 追踪尺寸线-延长线-箭头-引线(几何归属, 优于纯坐标聚合), 排除标题栏/技术要求, 歧义项标 `needs_review`。要求 PDF 有文字层; 纯扫描 PDF 会被拒绝。
- `detect_drawing_views.py`: 按尺寸箭头/引线端点 + 线稿划分视图, 补无尺寸视图, 排除 3D 模型, 输出每视图透明 PNG + `view-regions.json` + `view-review.pdf`。**视图边界由脚本客观给出, 取代 GLM 归因与手工坐标定位。**
- 该综合信息用途: ① 作为重绘侧权威(类似 dim-table, 但含物理特征与视图归属); ② **指导原图识别**——用重绘的"值+特征+视图"去原图对应物理特征处核销。
- 原图分流:
  - 原图是**带文字层的 PDF** → 走同样两个脚本流程, 得原图侧综合信息, 再与重绘侧核销。
  - 原图是**扫描件**(无文字层) → 依赖 GLM 识别, 但用重绘图综合信息做**反向核销指导**(对每个重绘尺寸, 专门提示 GLM 去原图对应特征找, 确认"原图有(数值可不同)"还是"原图无")。
- `build_dim_table.py` 仍可作为坐标聚合兼容工具, 但重绘侧首选上面的两个脚本流程。

### 客户原图侧也必须机器化建"视图尺寸清单"(强制, 2026-08-12 从 750683 提炼)

build_dim_table 只覆盖**重绘侧**。**客户原图若也有文字层(矢量 PDF 或带 OCR 层的扫描件), 必须用文字层坐标按视图区域(x/y 范围)机器化切分, 生成"原图视图尺寸清单"作为客户侧权威清单**, 禁止凭 GLM 分块读的归因人工整理客户视图清单。

- 方法: 对原图 `page.get_text('dict')` 提取带坐标 token, 按各视图的 x/y 区域(如 B-B 剖视 x700-1000/y50-700)筛选, 逐 token 列出 → 该视图的尺寸清单。文字层 token 不会"漏", 漏的是人工整理/GLM 归因不稳定。
- **双向核销**: 原图清单的每个值去重绘 dim-table 找, 重绘 dim-table 的每个值去原图清单找; 任一侧有值找不到即报出, 漏项机械拦截。
- 教训: 750683 原图 B-B 剖视 `R5±3`@x786,y516 在文字层完整存在, 但 GLM 分块读把它归到"主视图底部斜边", 人工整理 B-B 剖视清单时漏记, 初版报告漏掉该尺寸, 复核才补回(客户标注总数 59→60)。**原图侧尺寸清单必须机器化, 禁止凭 GLM 归因或印象整理**; 用"原图文字层按视图区域切分 + 与重绘 dim-table 双向核销"即可杜绝此类漏项。
- 同样教训适用于"视图边界归属": GLM 把客户独有/跨视图标注(粗糙度 Ra25、R5±3、25)归错视图时, 以文字层坐标落在哪个视图区域为准, 不被 GLM 的归因带偏。

### 禁止反复尝试与预算规则(round5 固化, 2026-08-13)

防止流程/子代理在"同一目标"上无限重试(round4 复盘: 探索式大量渲染大图、低清反复核验致单案例 126-180 分钟; 780673 基准仅 31 分钟)。

**强制规则(用户确认)**:
1. **同一目标重复尝试上限**: 对同一个尺寸/同一物理特征的识别, 裁剪+GLM 读**最多 2 次**(第 1 次直接读 → 第 2 次为唯一一次升级重试)。第 2 次仍读不清 → **立即停止, 标 `Needs manual confirmation`, 进入下一项**, 禁止第 3/4 次。
2. **升级阶梯(一次)**: 第 1 次读不清, 只允许**一种**升级手段(二选一): ① `enhance_image.py` 增强(CLAHE+放大) 或 ② 提升 DPI 到 800-900。**禁止反复改裁剪框/坐标碰运气**; 重试触发条件是"清晰度不足", 不是"框没框准"。
3. **裁剪先立项后执行(禁止探索式)**: 裁剪前必须写出**目标裁剪清单**(待核销尺寸 + 视图 + 理由), 一次批量裁剪 → 批量 GLM 读。**禁止"先裁一块看看有什么"的探索式裁剪**(round4 780314 渲染 120 张大图即此根因)。
4. **坐标定位禁止试错**: 重绘侧定位一律用脚本输出的 `view-regions.json` 视图边界 + 台账坐标, 不在整页图上手工找坐标。扫描原图上**禁止反复试坐标裁剪**; 用重绘台账的"值+特征"按物理特征找, 找不到就标人工, 不靠坐标穷举。
7. **故障重试固定**: GLM 429/超时固定重试 2 次后放弃该项; 网关 502 → **立即用 SendMessage 恢复上下文, 不冷启动**(round4 已验证有效)。
8. **低清/低对比件差异项核验上限(round6 固化, 2026-08-13)**: 低清/低对比扫描件(200dpi、低对比、英制小字)的**每个差异项**, 核验最多**"增强 + 读 2 次"**(第 1 次直接读 → 增强/升 DPI 后最多再读 1 次), 仍不清 → **立即标 `Needs manual confirmation`, 进入下一项**。**禁止对低清件做 20-40 张裁剪式的多轮核验**(round6 780672/780674 因多轮增强/二值化核验, 单差异项被读 3-5 次, 识别环节达 28-58 分钟)。判断依据: 每个差异项只保留"第 1 次读 + 第 1 次增强后读"两轮证据, 其余轮次的追加裁剪一律不做, 直接标 Needs manual。

**建议兜底(用户未强制, 按需采用)**:
5. GLM 调用按阶段配给(不设空泛总数): 分块读(按视图数, 每视图 1-2 次) + 反向核销(每条重绘尺寸 1 次定位) + 精读(仅 `needs_review`/客户独有值, 每人最多 1 次含升级)。
6. 时间盒兜底: 单案例总时长上限 60 分钟, 到点把未闭合项统一切 `Needs manual` 收尾。

> round4 基准: 780673 单案例 31 分钟(裁剪图 48 张、GLM 定向追问)。落实强制规则后单案例应控制在 1 小时内。

### 批量并行 GLM 与 15 分钟目标(round5 固化的性能规则, 2026-08-13)

**硬指标: 单案例盲审/审核墙钟目标 ≤15 分钟(用户 2026-08-13 要求)。**

**核心杠杆(实测)**: 批量并行 GLM。`batch_glm.py` 用 ThreadPoolExecutor 并行调 GLM-4.6V(默认 4 路, round4 曾 8 路可行)。
- 实测: 4 张裁剪图并行 4 路总耗时 21.6s(串行 ~71s), 加速 **3.3×**, 墙钟 = 最慢单张。GLM 每张 14-21s 是固定 API 成本, 并行化把"多张墙钟"压到"单张最慢"。
- 用法:
  ```bash
  py "${CLAUDE_SKILL_DIR}/scripts/batch_glm.py" <图1> <图2> ... --workers 4
  py "${CLAUDE_SKILL_DIR}/scripts/batch_glm.py" <裁剪目录> --glob "*.png" --workers 4
  ```

**15 分钟目标(单案例)**: 目标作用于**识别+核销**环节; 报告撰写不压缩, 保持完整答案级。
| 环节 | 分配 | 手段 |
|---|---|---|
| 读规范/台账 | 0-1 分 | skill 要点 + 台账摘要预加载进子代理 prompt, 不自己读 |
| GLM 视图标定 | 1-2 分 | 视图图一次 batch_glm 并行 |
| 裁剪核验(仅差异/独有/模糊项) | 2-4 分 | 裁剪清单集中批量 → batch_glm 并行 1-2 批 |
| 反向核销 | 1-2 分 | 带文字层原图用台账双向比对; 扫描件并入并行批 |
| **识别+核销小计** | **4-9 分** | ≤15 分目标在此环节 |
| 报告撰写 | 8-10 分 | **完整答案级报告, 不压缩**(按 answer-grade-standard 详细模式) |
| **总(含完整报告)** | **12-19 分** | 带文字层/清晰原图可 ~15 分内 |

**配套规则**:
1. **只核销差异项**: 重绘台账↔客户台账(带文字层)或清晰 Match 项, 双向比对直接定, 不 GLM。仅对: 客户独有/重绘独有/模糊读数/临界值(与重绘几乎相等)裁剪+GLM。
2. **裁剪清单集中 + 并行**: 一次列清单 → 批量裁剪 → 一次 batch_glm, 禁止"裁一张读一张"。
3. **临界值二次确认保留**: 客户读数与重绘几乎相等(|Δ|≤1 或 <0.5%)时, 并入并行批多读一次交叉验证, 不牺牲正确性。
4. **增强前置**: 低清/低对比局部先 enhance_image.py 再入批。
5. **报告不压缩**: 报告必须为**完整答案级**(按 answer-grade-standard 详细模式: 每尺寸一行 + 每视图小结 + 汇总 + ID 核销), 报告撰写时间不列入压缩目标。**禁止为赶 15 分钟而精简/省略报告内容**; 15 分钟目标只作用于"识别+核销"环节, 完整报告另计。
6. **原图类型分层**: 带文字层原图(两侧台账)与清晰扫描, 识别+核销 ≤9 分, 含完整报告可 ~15 分内; 200dpi 低清(如 780672)允许 15-20 分或增加 Needs manual 项, 不强求。

### 单案例并行流水线:报告两段式 + 原图三层核销(round5 固化, 2026-08-13)

**单案例执行流水线**(重绘侧与原图侧**自始并行**, 报告分两段写入):

```text
[并行开始](原图①与重绘脚本同时启动, 互不等待)
重绘侧:
  extract_dimension_ledger.py(~6s) → 尺寸台账(个数/数值/所在视图)
  detect_drawing_views.py(~1.5s) → 视图PNG → batch_glm 并行标定物理特征
原图侧:
  ① 原图整体自由读(batch_glm 分块并行, 1-2批) → 原图候选读数清单(只当索引, 允许漏读/臆造)
     · ① 不依赖重绘任何信息, 可随任务启动立即开始
  ｜
[重绘台账+视图就绪后]
报告骨架: 重绘台账 + 标定物理特征 → 报告 ~50%
原图侧续:
  ② 台账双向机器比对(0 GLM): 重绘台账↔原图候选, 按"数值+物理特征"自动匹配
     - 匹配 → Match 直接核销(通常 60-70%)
     - 独有/不一致/模糊 → 差异候选清单(通常 15-25 条)
  ③ 仅对差异候选 batch_glm 定向核验(裁剪+增强): 原图独有→确认; 重绘独有→确认"确无"; 临界值→二次读
[合并] 原图核销结论一次性更新进报告 → 报告 100%
```

**报告两段式写入(强制)**:
1. **第一段(重绘侧, ~50%)**: 脚本台账(尺寸个数/数值/所在视图) + GLM 标定的物理特征, 直接写入报告。此时报告已含"重绘侧 尺寸-物理特征-视图"完整骨架, 后续不需回头补。
2. **第二段(原图侧+核销, ~50%)**: 原图三层核销结论(客户读数、状态判定、差异证据)一次性补全进报告。
3. 报告仍为**完整答案级(不压缩)**: 两段式只改变写入顺序, 不减少内容(见"报告不压缩"规则)。

**原图三层核销(禁止逐尺寸调 GLM)**:
1. **整体自由读**(batch_glm 分块并行, 1-2批): 原图全部候选读数, 只当索引, 不要求完整/准确(漏读臆造正常, 后两层过滤)。**①不依赖重绘任何信息, 必须与重绘脚本同时启动, 不等待**。
2. **台账双向机器比对(0 GLM)**: 重绘台账每条 ↔ 原图候选, 按"数值+物理特征"匹配。匹配上的 Match 直接核销; 独有/不一致/模糊进差异候选。
   - **禁止对重绘表每个尺寸各调一次 GLM 去原图找**(如 78 条→78 次调用, 6-7 分钟, 毁掉 15 分钟目标)。台账过滤是 0 GLM 的纯代码步骤, 先滤掉 60-70% Match。
3. **差异候选定向核验**(batch_glm 并行, 2-3批): 仅对差异清单裁剪+增强+GLM, 确认"原图有(数值可不同)"/"原图确无"/"临界值二次读"。

**防锚定(独立怀疑, 强制, 2026-08-13 round5 提炼)**: 两个脚本锚点让审核者先知道重绘全部数值, 客户扫描读数一旦模糊, 易被重绘值"吸过去"(实测漏判: 780672 客户 420 被读成 419.1、Ø2221 被读成 2220.1; 780674 客户 (Ø1706.3) 被读成 1705.7, 全部误判 Match, 答案 error 被漏掉)。规则:
- **客户读数独立判定**: 对每个特征, 先基于原图自身读客户值(增强+裁剪, 不看重绘值), 再与重绘值比对。**禁止"带着重绘值去判客户"**——"客户值看起来和重绘一样"不得作为 Match 依据。
- **客户读数与重绘一致时仍要确认客户值本身可读**: 客户扫描清晰可读且=重绘 → Match; 客户扫描模糊(即使"像重绘")→ 标 Needs manual 或并入并行批二次读, 不默认一致。
- **独立怀疑默认**: 客户值≈重绘值的情形, 默认假设"客户可能不同", 用临界值二次确认(见配套规则3)交叉验证, 而不是默认一致。
- 三层核销天然防锚定: ①整体自由读在不知重绘值时完成; ②台账比对是机器代码(0 GLM)不产生锚定; ③差异定向核验先读客户值再比对。

## 平衡护栏与推断

把硬规则用于核验过程,而非强制某个读法。客户图纸仍具权威性,但重绘图可产生候选读法并佐证不清的源标注。绝不把重绘图当作客户数值的唯一证明。

证据优先级(从高到低):

1. 直接源证据,包括完整字符、符号、线条端点;
2. 几何、指引线、延伸线、相邻尺寸、尺寸链;
3. 同一物理特征上来自重绘图的佐证;
4. 计算、比例、标准、工程合理性;
5. 证据仍冲突时的人工确认。

低优先级证据可在与高优先级证据一致时补全缺失的扫描细节;不得静默覆盖清晰的客户标注。记录竞争性读法,并说明哪个证据支持所选读法。

## 建立特征对应

先建特征映射表再比较数值。每个条目按以下识别:

- 源视图或细节;
- 物理特征;
- 指引线或延伸线端点;
- 相邻几何;
- 视图方向与剖面方向;
- 关联的尺寸与备注。

允许客户细节视图在重绘图中合并进主视图。当特征与要求保持无歧义且完整时,视图组织的差异视为等价表达。

禁止仅因两个尺寸在页面上邻近就视为对应。在其指引线或延伸线控制同一物理特征之前,不得比较两个数值。同理,禁止用重绘文字层的坐标直接定位原图上的同一标注——两边坐标系无关,特征对应只能靠物理特征证据,不能靠坐标映射。

对每个拥挤或放大的细节,先建特征映射再读数:

1. 沿两条延伸线或完整指引线追踪到端点;
2. 用文字命名受控物理特征;
3. 读数字前先看尺寸线形状:
   - 两条延伸线之间的直线尺寸线+箭头通常表示线性长度/宽度/高度/厚度/偏移/间距;
   - 以顶点为圆心、跨两条射线的弧状尺寸线通常表示角度尺寸,如底部轮廓夹角;
4. 判定标注类型:线性、角度、半径、直径、弧长、或参考;
5. 然后才转写并比较数值及其符号。

尺寸线形状是强结构证据,不是唯一依据。半径、直径、弧长标注也可能与曲线几何相关,直线指引线也可能指向曲面。扫描件保留角度时,用弧尺寸线、两条边界射线、度或分符号确认角度。

禁止用相同数值作为对应证据。两个相同值的标注可能控制不同特征,如凸台高度与凸台顶部宽度。

在物理特征层做显式对应调和,而非视图数量层。一条客户标注可映射到一个或多个兼容的重绘表达,多个客户视图可合并为一个重绘视图。记录映射基数,不重复使用一条重绘标注满足两个不兼容的客户特征。

## 比较尺寸

对每个已映射特征,比较:

- 标称值与单位;
- 直径、半径、角度、弧、深度、数量符号;
- 双边、单边、极限、配合公差;
- 参考尺寸状态;
- 粗糙度与加工符号;
- 重复特征数量、节距、圆周分布;
- 基准、表面、剖视、细节引用。

尺寸端点与受控物理特征之间的清晰、唯一匹配即可直接比较。仅在特征拥挤、视图重排或对应模糊时,才从相邻几何、邻近尺寸、剖面方向或尺寸链寻求额外锚点。不强制固定锚点数量。清晰条目经视觉核验并记入尺寸台账后,除非后续尺寸链冲突、重复映射或矛盾发现直接影响该条目,否则不再重开或复查。

把标注限定符视为尺寸的一部分。尤其保留标记参考尺寸的尾随星号。仅标称值一致而无参考状态,不是完整匹配。

清晰公差直接比较,保留符号与上/下限位置。仅当扫描不清、小数点分隔符模糊、源图与重绘图冲突或量级不合理时,才用完整公差解析流程。此时保留原始文本与解析值分开,检查逗号与小数点,只测试视觉支持的移位,并与邻近几何、尺寸链、相关公差和引用的标准比较量级。不合理的量级触发复核;绝不改写清晰的源标注。

## 解析模糊手写

用分级流程并明确停止条件:

1. 查看原始源图和一个放大裁剪。若完整字符组清晰,停止。
2. 仍不清,旋转或调对比度一次,与同一绘图者其他笔迹比较数字形状。有一个读法变清晰即停止。
3. 仍有歧义,追踪尺寸端点,测试相邻几何或尺寸链。
4. 用重绘图作为最终消除歧义线索,但不假设其正确。
5. 证据仍不收敛,记录支持的各候选并标记 **需人工确认**,而非继续反复图像处理。

特别注意手写 `3/5`、`1/7`、`2/5`、`0/6`、`12/15`、`10/15`、小数点分隔符、度标记、分标记、直径符号、正负公差。

### 区分星号、度标记与旋转数字

在把任何小的凸起标记判定为度符号之前:

1. 与同一图纸上已知的星号和度标记比较;
2. 检查技术要求中是否有星号定义(如"尺寸仅供参考");
3. 寻找独立角度证据:角度几何、弧尺寸线、从顶点出发的两条射线、或清晰度标记;
4. 当延伸线测量宽度/高度/偏移时,拒绝角度读法。

跨两条射线的清晰弧尺寸线,加上可读的度或分符号,足以识别角度。仅当其中一个元素不清或缺失时,才寻求额外几何或重绘图佐证。

对旋转或拥挤的数字,在脑中或数字地旋转裁剪图,检查完整字符组。绝不把孤立的可见数字提升为尺寸,除非两条尺寸线端点都支持它。检查淡的前导数字是否与尺寸线重叠;常见错误是把 `13` 读成 `2` 或 `3`。

## 比较技术要求

仅当用户要求完整审核、技术要求复核、翻译、材料复核或制造要求检查时,才做完整技术要求比较。仅尺寸类请求就只比较尺寸、限定符、公差、数量和表面纹理,不扩展到标题栏或制造备注。

按语义映射客户备注到重绘备注,而非备注编号。检查铸造标准、表面质量、未注圆角、硬度、加工余量、缺陷限制、位置度控制、参考尺寸、检验规则、材料、划掉的要求。

当重绘图通过符号、标题栏字段、括号参考尺寸或其他等价惯例表达同一要求时,不报告备注缺失。

## 结果分类

仅使用以下比较类别:

- **Match(一致)**:同一特征且完整标注直接一致。
- **Confirmed error(确认错误)**:同一特征成立;数值或要求冲突。
- **Confirmed omission(确认遗漏)**:客户要求存在;重绘图任何地方都无等价表达。
- **Equivalent expression(等价表达)**:内容通过不同视图或制图惯例保留。
- **Needs manual confirmation(需人工确认)**:源图仍模糊或特征对应不完整。

## 报告

按用户请求选择报告模式:

- **简洁模式**:只报告确认错误、确认遗漏和实质性需人工确认条目。
- **详细模式**:覆盖每个客户视图和每条尺寸标注,包括正确匹配的尺寸。每视图一个子节。读取 `references/audit-checklist.md`,使用其完整尺寸台账结构。

用户未指定模式时,差异核查优先简洁模式,完整审核/完整比较/每一条尺寸时优先详细模式。

详细审核报告命名 `<图纸号>-审核报告.md`,如 `ZM-786-审核报告.md`。用重绘图标题栏中的图纸号;不含文件前缀、内部材料号或描述性零件名,除非它是该图纸号的一部分。

把详细审核归档与对话摘要分开:

- 详细归档是人工复核记录。保留每视图尺寸台账、物理特征、重绘对应、比较状态和必要证据备注。
- 不向详细归档添加独立的标题栏与要求比较表。仅当用户明确要求时才翻译或分析技术要求。
- 对话回复是简洁输出:呈现优先发现,最后加一句简短总体补充。
- 不在多个摘要节重复完整尺寸台账或复述相同发现。

详细模式以客户图纸的视图组织为报告主干。对每个客户尺寸,说明:

1. 识别的客户数值、符号、公差;
2. 它控制的物理特征;
3. 对应的重绘视图(即使与客户视图不同);
4. 匹配的重绘数值或等价表达;
5. 比较状态;仅对错误、遗漏或不确定条目包含对应证据;
6. 任何不确定性或建议动作。

用 **需人工确认** 状态和简短证据备注表达真正的歧义,不用百分比。

不要求视图数量相等。五视图客户图可能对应三视图重绘图,当信息已合并或重排。只在每条客户标注都追踪到无歧义的等价表达后才接受这种对应。显式记录未匹配、有歧义、重复、仅重绘图的标注。绝不从详细报告省略正确匹配的尺寸。

面向发现输出时,先列确认错误和遗漏。每条发现包含:

1. 客户数值或要求;
2. 重绘数值或状态;
3. 物理特征和视图;
4. 建立对应的证据;
5. 建议修正。

然后列出等价表达、需人工确认条目和抽查成功的代表性尺寸。说明扫描局限,但不用它来掩盖薄弱结论。

生成标注 PDF 前,先定稿发现清单。然后渲染结果,确认每个框都指向目标标注。

### 答案级报告规范(2026-08-11 从 5 份答案报告提炼)

**审核报告必须达到答案级标准**。完整规范(结构/状态分类/物理特征判断/漏判清单/等价表达/参考属性口径)见 `references/answer-grade-standard.md`, 报告前必须读取并遵守。

核心要点:
- 按客户视图分节, 每尺寸一行: 客户标注|物理特征|重绘对应|状态|证据。
- 6 类状态严格: Match/Equivalent expression/Difference to note/Confirmed error/Confirmed omission/Needs manual confirmation。
- 物理特征具体名称, 禁止泛称。
- 必须捕获: 基准/几何公差(同轴度/跳动)/参考尺寸属性/数量/图案说明。
- 参考属性移除、小数值差异、重绘新增必须逐项列出。
- 数量核销精确到合计=总数。
- **ID 完整性核销(强制, 2026-08-12)**: 报告完成后, 统计报告的 ID 总数(V1-Dxx 等), 与客户标注总数比对, 缺项必须补回。**防止两类系统性漏项**:
  1. 同值尺寸合并: 数值相同但物理特征不同(如 1 窄板宽 vs 1 板厚、6 槽高 vs 6 定位), 必须按特征分别列出, 禁止合并。
  2. 局部放大视图整节遗漏(如 V5 的 1/8、R1/8): 即使未作为重绘独立视图保留, 其尺寸须在合并视图逐一核销。

### 常见漏判/差距清单(2026-08-11 RED 阶段从答案对照提炼)

以下是我在 P1001 案例对比中暴露的具体错误模式, 审核时必须避免:

1. **漏判 omission**: 客户有独立标注、重绘无对应时, 必须逐项核对尺寸界线端点确认是 omission, 不能因"重绘数值相似"就跳过。案例: 客户 Ø1515(上口外圆) 重绘无, 不能拿重绘 Ø1460 顶替。
2. **漏判参考角度**: 客户参考角度(如 60°* 扇区角度)重绘无, 需单独确认 omission, 不能漏。
3. **状态判定过弱**: 基准直径名义值/公差/参考属性都不同时, 是 Confirmed error 不是"需人工确认"。案例: 客户 Ø2221(-1.5) vs 重绘 (Ø2220.1 +0/-15) → Confirmed error。
4. **漏判数值差异**: 客户 420 vs 重绘 (419.1) 是 Confirmed error, 不能漏。
5. **漏判局部视图 omission**: 局部放大图的尺寸(如凸块顶部平段 22、肩部 5)必须逐一核对, 重绘局部图缺失标 omission。
6. **参考属性漏核**: 客户带星号*的参考尺寸, 重绘若移除*或改括号, 是 Confirmed error(数值同但属性变)或 Difference to note。
7. **数量核销不精确**: 必须精确到每条状态, 且状态合计=客户总数。
8. **未查重绘新增标注**: 重绘新增/合并的标注(如 (5×)R25、参考高度)必须专节列出, 不计入客户总数。

### 答案级报告补充规范(2026-08-11 从 860404-TE001 提炼)

1. **等价表达广泛识别**: 客户独立尺寸若由中心对称(中心线+总长)、尺寸链拆分、分数/十进制转换、槽深由文字改截面尺寸表达, 均判 Equivalent expression, 不误判为 omission。案例: 客户 18 1/4 → 重绘"中心线+36 1/2"对称。
2. **同值不同特征必须分开核销**: 数值相同但尺寸界线端点/物理方向不同的尺寸, 分别记录, 不合并。案例: 两个 6、两个 3 1/2 控制不同特征。
3. **每视图加"小结"**: 每视图尺寸表后加一段小结, 概括该视图匹配情况。
4. **图案/技术说明纳入**: 图案槽宽(0.75"→3/4)、槽深(3/8 DEEP GROOVED→侧视3/8)、阵列间距(4.750→4 3/4)都要核销, 通常为等价表达。
5. **重绘新增必须追溯来源**: 重绘新增尺寸(如 30、1 1/4)不能反向当客户要求, 需说明"应追溯模型或检验依据"。
6. **配合/工艺文字提示**: 客户配合文字(如 BORE TO FIT)重绘未重现时, 作为提示记录, 不算尺寸漏标。

### 答案级报告补充规范(2026-08-11 从 780673-ZM-786 提炼)

1. **几何公差必须捕获**: 同轴度(◎ A Ø3)、跳动、位置度等公差框及基准, 必须识别并核对。案例: 客户 `◎ A Ø3` 顶部开口轴线同轴度, 重绘一致。
2. **Tip./Ref. 属性单独核对**: 客户 Tip.(典型)、Ref.(参考)、括号参考在重绘中若被省略, 列 Difference to note(数值同)或"存在差异"(数值也变)。
3. **小数值差异判 Difference to note**: 客户 Ø437.62 vs 重绘 Ø437.6(差0.02)、239.8 vs 239.9(差0.1), 参考尺寸的小数值变化不判为错误, 列 Difference to note。
4. **重复表达按等价**: 客户在主剖视和俯视图重复表达同一特征(如 40 Tip. 筋宽), 重绘集中在单视图表达 → 等价表达。
5. **典型(Tip.)展开**: 客户 R5 Tip.(1处典型) 重绘展开为两处显式 R5 → 等价表达。

### 答案级报告补充规范(2026-08-12 从 780672 提炼)

1. **重绘尺寸表自身可能漏条目，必须与重绘 GLM 视觉读交叉核对**：
   - build_dim_table 的底部 y 过滤会把俯视图下部的间距/孔径尺寸误当"标题栏"丢弃（780672：俯视图吊耳间距 254/190/125.2 及 10° 的公差 ±5' 全部落在 y>660 带，整块丢失）。已改为页高相对截止 + CJK 标签过滤 + 自动跳过图号。
   - **"重绘无 X" 断言前必须同时查 dim-table 和重绘 GLM 视觉读**，并排除 dim-table 过滤/文字层缺失造成的假缺失。重绘 GLM 视觉读捕获了 254/190/125.2，而旧 dim-table 没有 → 说明 dim-table 漏了，不是重绘无。
   - 公差 `+0/-15` 在文字层常缺 `+` 号（只有 `-`/`0`/`15` token），dim-table 输出 `0-15`，GLM 也只读 `-15`；需裁剪重绘区域视觉核验补全。
2. **低清扫描件（≤200dpi）GLM 自由读默认不可信**：780672 客户 200dpi 扫描上 GLM 臆造大量数值（765←745、48×10.5、7×10.5、6×R25、280、134、多处 √Ra12.5、假基准 A/B），与答案完全不符。提示词"禁止编造"不足以约束。规则：**自由读结果只当候选线索，客户读数必须以重绘尺寸表按物理特征核销 + 裁剪放大核验为准**；客户独有值（重绘无此值，如 Ø1515、60°*、凸块 22/5）必须裁剪客户图放大核验后才能写入报告。
3. **基准符号臆造**（780314"假基准 2"的同类复发）：GLM 把剖视/向视图名称字母（A-A/B-B）、中心线误读为"基准A/基准B"。规则：**基准必须看到三角形基准符号+字母图形**；图上只有 A-A/B-B 等剖视名称而无可辨认三角形基准符号时写"无基准"。反例：答案 780672 客户图无任何基准符号。
4. **直径链并列必须逐条列出**：主视图顶部常并列多个直径（780672：Ø1515 独立于 Ø1440 正上方，另有 Ø1460/Ø2089），GLM 只读最大或只读其一。每个直径按尺寸界线端点分别核销。

### 答案级报告补充规范(2026-08-12 从 780314 提炼)

1. **多个形位公差框必须逐个枚举**：全跳动与同轴度/同心度可同时存在（如 780314 全跳动 1.5|A + 同轴度 ◎Ø3|B），GLM 易只读其一，必须逐个列出公差值+基准+受控特征，防止漏判 Confirmed omission。
2. **附加视图即使只有 1 个标注也不能丢**：侧视/向视/局部放大视图可能只有一个参考尺寸（780314 上口侧视图 17 REF），必须单独成节核销；原图 GLM 漏读时，用重绘尺寸表（17Ref）反向核销补回。
3. **粗糙度逐处计数**：相同 Ra 值出现在多个表面（780314 下部外锥面+底部斜端面两处 Ra 12.5）分别列出并计数，不能合并。**注意反向风险**：计数指令易使 GLM 凭对称性臆造粗糙度标注（780314 曾从 2 处臆造到 8 处），只认实际画出的符号所在表面。
4. **GLM 自由读的噪声读数必须经尺寸表过滤**：小/模糊标记易被编造（780314：40.2←40+0/-2、55.5←555±2、51.5°←51°50′、假基准"2"、假 R10.5）；任何客户读数都以尺寸表反向核销为准，核销不上的值不得写入报告。

### 答案级报告补充规范(2026-08-12 从 780673 round2 提炼)

1. **高密图纸整图读会退化重复，必须分块读**：客户 300dpi 全图单次 GLM 读在第 25 项后陷入重复循环（同一行刷屏 N 遍），后续视图全漏；2×2 分块读可恢复同轴度框（◎AØ3/◎BØ3）、参考尺寸、(6)、B-B/详图全部标注。规则：整图读退化重复或视图数明显少于预期时，按 2×2/视图边界分块重读再合并（提示词已加"高密图纸分块读取"）。
2. **Tip. 省略 ≠ Ref./括号移除，是两个不同状态**：客户 **Tip.(典型) 属性被重绘省略（数值可能相同）判"存在差异"**（780673 五例：Ø60 Tip./R200 Tip./B-B R10 Tip./40° Tip./挂耳 40 Tip）；客户 **Ref./括号参考属性被移除判 Difference to note**（六例：(Ø437.62)/145.5 Ref/239.8 Ref/250.3 Ref/(6)×2）。旧规则"数值同→Difference to note"把两者混为一谈，必须按属性类型区分。
3. **Tip. 省略若重绘有显式替代表达 → 等价表达，不是存在差异**：80 Tip/40 Tip 筋宽→"18槽均布"、R5 Tip→两处显式 R5。
4. **尺寸表字号角色倒置风险**：尺寸文字字号可能小于粗糙度/GD&T/标题栏文字（780673 尺寸 10.8-11.7、粗糙度 12.1）。build_dim_table 已改为"公差符号±+/- + 邻近小字"分类，不再依赖固定字号阈值。重跑案例前先确认尺寸表包含直径公差与 R/角度尺寸，缺则检查脚本版本。

### 答案级报告补充规范(2026-08-12 从 780674 round2 提炼)

1. **公差归属优先"同行右侧邻接"，欧氏最近作兜底**：CAD 公差的标准布局是公差紧跟所修饰尺寸值右侧同一行（如 `1070 ±7.5`）。纯欧氏最近主值会把右下方/右上方的别的主值判为"更近"而抢走公差（780674：±7.5(x899,y187) 欧氏最近是 R10 TYP(x904,y208)，实际属于正左侧同行的 1070）。build_dim_table 已改为：片段与主值 **|dy|≤15 且 片段x0 − 主值x1 ∈ [-10,+20]** 视为同行邻接、优先归属，否则回退欧氏最近。**写报告前检查尺寸表里每个公差都挂在正确的直径/长度上**，若发现公差挂错主值（如 R10 TYP±7.5 这种语义不通的组合），说明脚本版本旧或归属规则有误。
2. **底部标题栏截止必须分 x 区间，不能全局 y 截止**：本批 CAD 出图的标题栏都在右下角（CJK 标签 x0 全部 ≥684），而视图标注可能延伸到左下角 y>页高-100 区域（780674：D 俯视图 22.5°(y751)/45°(y782) 被全局截止丢掉；780314：30°(y763) 同样被丢）。build_dim_table 已改为：从底部 CJK 标签动态算标题栏左缘，只对 "y0>页高-100 且 x0≥标题栏左缘-20" 的 span 判为标题栏；页底最下行(页高-25 以下)的单字符数字判为图框坐标格丢弃。**"重绘无 X" 断言前必须查最新尺寸表 + 重绘 GLM 视觉读**，若尺寸表底部角度/间距缺失而 GLM 视觉读到，先怀疑尺寸表过滤，不要直接判 omission。
3. **低对比度高分辨率原图的分块读仍会把中心线端点字母误读为基准**：780674 客户 300dpi 分块读 tile-00 报"基准A(右侧竖直中心线)/基准B(左侧竖直中心线)"，裁剪该位置放大核验确认无字母、无三角形 → 写"无基准符号"。已有"基准=三角形符号防伪"规则仍须配合裁剪核验执行，不能只凭分块读的自由输出。

### 答案级报告补充规范(2026-08-12 从 860404 round2 提炼)

1. **英制分数尺寸会让 build_dim_table 碎化，必须用最新版(含 merge_fractions)重跑**：CAD 出图把分数拆成整数/分子/分母/英寸符 `"` 独立 span（`24 7/8`→`24`+` 7`+`8`；`3 5/8`→`3 `+`5`+`8`；`37 3/16`→`37`+` 3`+`16`），旧版把它们当多个独立主值，860404 尺寸表碎片化到 164 条，英制分数的"重绘→客户反向核销"完全失效。修复后 83 条、54 个分数全部合并。**拿到英制/分数图纸先确认尺寸表含完整分数（`5 1/4` 而非 `5`+`1`+`4`），缺则检查脚本版本**。分数 token 三种来源：合并型 `W N`、前置空格 ` N`(分子)、后置空格 `W `(整数)、裸数字分子；分母与分子同行右侧(gap 1-13px)或正下方同 x；`"` 英寸符文本有 `"`/` "`/`" ` 三种变体，near_inch 判定必须 strip。
2. **裸三角形(△/▽)不是形位公差也不是基准**：860404 扫描件尺寸旁有大量裸三角形标记，GLM 反复误判为"位置度△1/△38、平面度△6、平行度△4 1/2"。裁剪核验确认：无矩形公差框、三角形内无字母 → 答案判"无形位公差框、无基准"。只有"矩形框内三角形"才是 GD&T、"三角形内字母"才是基准；孤立△/▽ 是铸造/加工/指引标记。
3. **图案/阵列视图的边界字母(A/B/C)不是基准**：860404 花纹图四周的独立 A（上边界）、B（左右边界）是图案定义方向/范围标识，分块读误报"基准 A/B"。裁剪核验确认 A/B 为独立字母、不在三角形内 → 写"无基准符号"。这是"基准=三角形符号防伪"的又一变体，须配合裁剪核验执行。
4. **英制图纸的直径符号 Ø 常为矢量图形不在文字层**：860404 重绘文字层无任何 Ø 字符（Ø5/Ø2 1/2/Ø4 3/4 的 Ø 是图形），尺寸表只含数值，Ø/R 前缀由重绘 GLM 视觉读补充。`R` 前缀是文本（`R1"`/`R3` 在文字层，`R1/4` 的 R 是独立小 span 会被单字符过滤丢弃）——数值核销以尺寸表分数合并后的主值为准，Ø/R 交给 GLM 视觉读核对。

### 答案级报告补充规范(2026-08-12 从 780314 round3 复验提炼)

1. **check_completeness.py 必须带 UTF-8 reconfigure**（与 glm_vision.py 同类）：Windows GBK 控制台/重定向下，未核销分支打印 `⚠` 直接 UnicodeEncodeError 崩溃，核销结果全部丢失。复验时发现并修复（脚本头部已加 stdout/stderr UTF-8）。**跑 check_completeness 前先确认脚本已含 reconfigure，崩溃先查此点**。
2. **check_completeness 的名义值碰撞为已知小局限，不影响报告**：角度 `20°±1°` 与半径 `R20` 的名义值同为 "20"，核销时角度会先消耗掉该名义值，导致 R20 被误报"GLM 漏读"。判定"重绘无 X"仍以"尺寸表 + 重绘 GLM 视觉读"双查为准（见 780672 规则），核销器单侧误报不足以判 omission。

完整复查清单与可复用比较表,读取 `references/audit-checklist.md`。

### 报告必须包含耗时统计

每次审核任务结束时,报告(详细归档和对话回复)都必须报告**本次审核任务花费的时间**。时间从任务开始(收到审核请求)到任务完成(报告产出)。

在报告末尾追加一节:
```markdown
## 耗时

本次审核任务耗时: **X 分 Y 秒**
(从收到图纸请求到完成报告)
```

对话回复末尾也要附一句耗时说明。这样用户能了解每次审核的用时。
