---
name: engineering-drawing-audit
description: 审核客户工程图纸与 SolidWorks/CAD 重绘图的尺寸、公差、符号、视图、技术要求差异，生成审核报告。适用于扫描件、手写、旋转、多语言、低清晰度 PDF。当用户要求对比两份图纸的尺寸/公差/符号/指引线/剖面视图/放大细节/合并视图/技术要求/材料/制造备注，或要求生成核实的差异报告/标注审阅件时使用。
---

# Engineering Drawing Audit (工程图纸审核规范)

以物理特征对应关系审核两份工程图纸。客户图纸作为唯一权威需求来源，重绘图仅在手写不清时作为佐证。

---

## 架构与本地化多模态说明

本 Skill 已实现**全本地化执行**，无需外部第三方视觉 API Key：
1. **重绘侧/双侧机器化锚点**：由本地 Python 脚本直接解析 CAD 文字层与几何元素，毫秒级提取 100% 权威的尺寸台账并自动划分视图区域（0 API 消耗、0 视觉幻觉）。
2. **高保真切图与图像增强**：由本地 PyMuPDF 和图像处理脚本生成 300~800 DPI 高清渲染图与 CLAHE 对比度增强切图。
3. **原生多模态视觉审图**：由 Agent 直接调用原生高分辨率视觉多模态能力审阅图纸与局部切图，严格遵循工程图专业提示词与物理特征标定规范。

---

## 0. 工作空间与项目归档规范（铁律）

为保证工程审图文件的规范性与可追溯性，所有审核任务必须严格遵循以下目录结构：

```
<工作空间根目录>/
└── 图纸审核/
    └── <厂内图号>-<零件名称>/           # 例如: 图纸审核/780683-定锥-452.0832/
        ├── ledger_customer/            # 客户原图尺寸台账与对账底稿 (review.pdf)
        ├── ledger_redraw/              # 重绘图尺寸台账与对账底稿 (review.pdf)
        ├── views_customer/             # 客户原图切分视图与 crops/
        ├── views_redraw/               # 重绘图切分视图与 crops/
        ├── crops/                      # 600~800 DPI 关键特征局部放大切图 (如有)
        └── <厂内图号>-审核报告.md        # 最终输出的权威审核报告
```

- **执行要求**：启动审核时，首先在 `图纸审核/` 下创建独立的 `<厂内图号>-<零件名称>/` 工作文件夹，**审核过程产生的所有中间文件、切图及最终报告一律存放于该文件夹内**。

---

## 1. 审核模式判定与核心工作流

在启动审核前，首先检测两份图纸的文字层属性：
- **模式 A（双矢量图模式）**：原图与重绘图**均包含完整矢量文字层**（均为 CAD/SolidWorks 直接导出的电子 PDF）。
- **模式 B（单矢量/光栅扫描模式）**：重绘图含文字层，客户原图为**光栅扫描件/手写图/低清无文字层 PDF**。

```text
                    ┌─────────────────────────┐
                    │      输入两份图纸 PDF     │
                    └────────────┬────────────┘
                                 │
                    检测客户原图是否包含完整文字层?
                                 │
                ┌────────────────┴────────────────┐
                ▼ (是: 包含文字层)                  ▼ (否: 扫描件/无文字层)
        【模式 A: 双矢量图流程】             【模式 B: 单矢量/光栅流程】
   ┌──────────────────────────┐      ┌──────────────────────────┐
   │ 1. 双侧机器提取尺寸台账   │      │ 1. 重绘侧提取尺寸台账与视图│
   │ 2. 双侧自动切分独立视图   │      │ 2. 原图 300 DPI 全局渲染 │
   │ 3. 分视图拓扑绑定物理特征 │      │ 3. 物理特征引线追踪独立读数│
   │ 4. 补齐台账 JSON 语义    │      │ 4. 三层核销法 (快速核销/ │
   │ 5. 基于特征语义比对与分流 │      │    差异锁定/高倍CLAHE复核)│
   └────────────┬─────────────┘      └────────────┬─────────────┘
                │                                 │
                └────────────────┬────────────────┘
                                 ▼
                    ┌─────────────────────────┐
                    │  非文字图形符号核验      │
                    │  (GD&T框/基准/表面粗糙度)│
                    ├─────────────────────────┤
                    │  输出 100% 权威审核报告  │
                    └─────────────────────────┘
```

---

## 2. 核心审图规范与特征标定指令

Agent 在对工程图纸及局部切图进行视觉审阅时，**必须严格执行以下专业审图规则**：

### 2.1 必须完整捕获的 8 类标注
1. **尺寸标注**（线性/直径/半径/角度/弧长）：完整读出数值+符号（$\varnothing$ 直径、`R` 半径、`°` 度分秒、`弧长` 跨度）。
2. **公差**：尺寸公差（如 `+0.2/0`、`±0.1`、`+2/0`）和角度公差（如 `±5'`、`±0.5°`）。
3. **参考尺寸**：带 `REF`、星号 `*` 或括号 `()` 的标注，必须记录其参考属性。
4. **基准符号**：如基准 A、基准 B（**必须看到三角形基准符号+框内字母的图形**，孤立字母或剖视名不是基准）。
5. **形位公差框（GD&T，重点仔细搜索）**：
   - 全跳动（符号 $\text{Ⓣ}$ / ⌰ / $\nearrow\nearrow$）/ 圆跳动（$\nearrow$）
   - 同轴度（符号 $\odot$ ◎）、同心度
   - 位置度（符号 $\bigoplus$ ⨁）、垂直度（$\perp$）、平行度（$\parallel$）、圆度（$\bigcirc$）、平面度（$\Box$）
   - 必须读出：**公差值 + 基准字母 + 受控物理特征**。
   - **多个形位公差框并存**：必须逐个枚举，禁止只列其中一个。
   - **框内"公差值+基准字母"组合是形位公差框，不是独立基准符号**。
   - **裸三角形/倒三角形 ($\Delta$/$\nabla$) 不是形位公差也不是基准**：扫描件尺寸旁常见的孤立 $\Delta$/$\nabla$ 是铸造/加工标记或指引符号，禁止判为形位公差。
6. **表面粗糙度**：逐处列出并说明所在表面（BNIF 铸造标准、ANSI `250`/`500`、ISO `Ra` 值）。相同 Ra 值出现在多个不同表面时，分别列出并计数。
7. **数量**：如 `2-Ø40`（2 个孔）、`6×R25`（6 处圆角）、`(5×)R25`、`8X`。
8. **Tip./Ref. 属性**：典型 (`Tip.`/`TYP`)、参考 (`Ref.`/`REF`/`*`/`()`) 标注。

### 2.2 物理特征判断要求（禁止泛称）
- 每个标注必须用**具体物理特征名称**（如"上口外圆直径"、"波形法兰斜边长度"、"主外锥面"、"底部内圈直径"、"凸台高度"、"吊耳孔距"）。
- 依据**尺寸界线端点、引出线端点、尺寸线几何形状**判定，禁止使用"长度"、"直径"等模糊泛称。

### 2.3 低清扫描件防伪与防臆造原则（铁律）
- 当图片是低清扫描件（线条发虚、数字粘连）时，**初次自由读出的数值作为"候选线索"，必须经局部高 DPI 放大与尺寸台账反向核销确认**。
- **高频误读防范**：
  1. 避免把近似数字读成整十整百（如 `745` 误读为 `765`）；
  2. 直径链并列时（如主视图顶部并列 `Ø1515` / `Ø1440` / `Ø1460`），必须沿每条延伸线逐条核销，不可漏读；
  3. 严禁臆造数量乘号（如 `48×10.5` 凭空出现）；
  4. 剖视图名称字母（`A-A`、`B-B`）、中心线端点字母、花纹图边界字母（`A`、`B`）**均不是基准符号**，未见到三角形基准图符时一律记为"无基准"。

### 2.4 带公差/小字号标注自主触发 600~800 DPI 高倍放大切图复核机制（授权自主执行）
- **物理成因**：工程图中的公差数字（上下偏差、极限公差、度分秒角标）通常采用比基本尺寸小 30%~50% 的上下标小字号排版（如 `+6/-3`、`+5/-3`、`±30'`、`+0.15/0`）。在 300 DPI 全局扫描图上极易因像素压缩产生笔画粘连或视觉误判（如 `+6` 误读为 `+5`）。
- **自主触发铁律**：
  1. 凡是涉及**极限偏差、不对称公差、紧配合公差、复合角度公差**的尺寸项；
  2. 凡是初步视读与重绘图存在公差数值分歧（如 6 vs 5, 0 vs 2）的特征；
  3. **无需等待人工提醒，Agent 必须自主触发运行 `render_pdf.py` 或 PyMuPDF 进行 600~800 DPI 局部高倍放大切图（配合 CLAHE 增强）**，以微观像素级目视确认真实公差数字，杜绝任何字形混淆！

---

## 3. 本地工具链与执行命令

脚本位于 Skill 目录下的 `scripts/`，直接使用本机 `py` (Python 3.13) 执行：

### 3.1 尺寸台账与视图切分
```bash
# 1. 提取客户原图台账 (适用于模式 A)
py <SKILL_DIR>/scripts/extract_dimension_ledger.py "<客户原图.pdf>" --unit mm -o "图纸审核/<项目名>/ledger_customer"

# 2. 提取重绘图台账 (模式 A & B 通用)
py <SKILL_DIR>/scripts/extract_dimension_ledger.py "<重绘图.pdf>" --unit mm -o "图纸审核/<项目名>/ledger_redraw"

# 3. 自动划分视图并生成每视图透明高清 PNG
py <SKILL_DIR>/scripts/detect_drawing_views.py "<图纸.pdf>" --ledger <输出目录>/ledger/dimension-ledger.json -o "图纸审核/<项目名>/views"
```

### 3.2 PDF 渲染与局部高 DPI 裁剪 (模式 B 核心)
```bash
# 渲染整页为 PNG（默认 300 DPI）
py <SKILL_DIR>/scripts/render_pdf.py "<图纸.pdf>" "<输出目录>/render" --dpi 300

# 裁剪局部高倍微观图（对公差角标、形位公差框用 600~800 DPI）
py <SKILL_DIR>/scripts/render_pdf.py "<图纸.pdf>" "<输出目录>/crops" --crop <页码> <x> <y> <w> <h> --dpi 600
```

### 3.3 图像对比度增强 (CLAHE)
```bash
py <SKILL_DIR>/scripts/enhance_image.py "<裁剪图.png>" --method clahe --gray --target-width 1600 --out "<增强图.png>"
```

### 3.4 核心脚本维护与 GitHub 远端同步纪律
- **禁止私自改动**：Agent 严禁自主重构或随意修改 `extract_dimension_ledger.py`、`detect_drawing_views.py` 等底层算法脚本；
- **统一云端维护**：所有核心脚本迭代由用户通过 GitHub 官方仓库统一维护：`https://github.com/yu-hx-tom/engineering-drawing-audit.git`；
- **同步机制**：当用户提示脚本在 GitHub 有更新时，Agent 通过 `git pull` 或拉取远端文件覆盖同步并执行单元测试。

### 3.5 思路一核心防护：视觉主审 + 脚本安检门阻断机制 (Pre-Report Security Gate)
针对审图中**“同名数值污染视觉记忆”**（如客户图有多个 30、15、25，因重绘图有同名数字导致视觉误以为匹配）的致命缺陷，必须严格执行思路一两道防线：

1. **第一道防线：同名尺寸实体引线强行解耦**：
   - 凡是图纸上出现重复出现的同名数值（如 30、15、25、45° 等），视觉核销时**严禁仅凭数值配对**！必须强制显式写明其物理特征（如 `30 [止口槽深]` vs `30 [SECTION A-A 焊接吊耳高]`）；重绘图每个尺寸只能核销 1 处物理实体，绝不允许一抵二。
2. **第二道防线：下笔写报告前强制运行安检门 (`audit_gate_check.py`)**：
   ```bash
   py <SKILL_DIR>/scripts/audit_gate_check.py "图纸审核/<项目名>/ledger_customer/dimension-ledger.json" "图纸审核/<项目名>/ledger_redraw/dimension-ledger.json" -o "图纸审核/<项目名>"
   ```
   - **拦截逻辑**：
     - 若同名数值频次不一致（如客户图出现 3 次，重绘图仅出现 2 次），直接亮红灯报警！
     - 若客户图存在未被对消的残差项（如漏注 15°、30、1097.4），安检门**强行阻断报告输出**！
   - **拦截动作**：Agent 必须停下来，调取对应坐标的切图进行针对性“二次视觉复审”，确认为重绘图真正漏注后，必须在报告头部作为 `Confirmed omission` 加粗警告，严禁写出虚假的“零漏绘、零装配冲突”结论！

---

## 4. 比较规则与结果分类标准

### 4.1 6 类标准状态分类（严格）
| 状态分类 | 判定标准 | 典型示例 |
| :--- | :--- | :--- |
| **Match** | 同一物理特征，完整标注数值、公差、符号完全一致 | 客户 `Ø1440` $\leftrightarrow$ 重绘 `Ø1440` |
| **Equivalent expression** | 几何与公差信息完整保留，但采用不同视图或工程制图惯例表达 | • 客户十进制度数 `32.97° (-0.02°/0°)` $\leftrightarrow$ 重绘度分秒 `32°58' (-1'/0°)`<br>• 均布表达换算（如 `(6x60°)` $\leftrightarrow$ `6对吊耳圆周均布`）<br>• Tip. 展开为多处显式标注 |
| **Difference to note** | 实质数值一致，仅参考属性变化、极小数值微调或公英制四舍五入 | • 公英制转换保留一位小数（如客户 `2844,8mm ±6,35` $\leftrightarrow$ 重绘 `2844.8 ±6.4`，相差 0.05mm）<br>• 客户带 `Ref.` 重绘改为括号 `()`；客户 `(Ø437.62)` $\leftrightarrow$ 重绘 `Ø437.6` |
| **存在差异** | 同一特征数值一致，但关键属性被省略（**特指 Tip. 典型标记被省略**） | 客户 `Ø60 Tip.` $\leftrightarrow$ 重绘 `Ø60`（未注明典型也未在别处展开） |
| **Confirmed error** | 同一物理特征，数值、公差、符号或受控方向明确冲突 | • 客户 `115 (+6/-3)` $\leftrightarrow$ 重绘 `115.7 (+5/-3)`（名义值与公差双重冲突）<br>• 客户 `60°54'` $\leftrightarrow$ 重绘 `65°9'`（相差 4°15'） |
| **Confirmed omission** | 客户图纸明确存在的尺寸/公差/要求，重绘图完全无等价表达 | 客户独有 `Ø1515`、客户局部放大图 `22`、制造公差 `165 ±1.5` 在重绘图漏注公差 |
| **Needs manual confirmation** | 原图严重模糊、残缺或涂改，即使高 DPI 增强后仍无法唯一确定 | 无法确定的手写字符或断裂尺寸线 |

### 4.2 标题栏理论重量核销准则
- 标题栏理论重量对比（如客户 `1368.36 kg` vs 重绘 `1293.1 kg`，或 `8248 lbs` vs `3671.2 kg`）作为**实体模型壁厚或角度变动的辅助证据链**，必须在报告结论中进行说明。

---

## 5. 报告规范（工业答案级标准）

详细审核报告统一保存至对应项目文件夹内，命名为 `<厂内图号>-审核报告.md`。

### 报告必须包含的 5 大结构模块：
1. **审核基本信息与对象**：声明客户原图与重绘图品牌、零件名、图号、版本、材料、理论重量及审核模式（模式 A 或模式 B）。
2. **结论摘要与关键冲突排查 (Priority Findings)**：
   - 状态分布统计表（Match / Equivalent / Difference / Confirmed error / Confirmed omission 项数与占比）；
   - 使用 GitHub Alert (`[!CAUTION]` / `[!IMPORTANT]`) 突出显示关键冲突项与修改建议。
3. **按视图逐项核销清单**：
   - 按客户主视图、俯视图、局部放大图等逐视图建立核销表格；
   - 表头格式：`序号 | 客户原图标注 | 所控制的物理特征 | 重绘图对应标注 | 状态定性 | 差异证据与说明`。
4. **【表面粗糙度与表面质量核销专节】（强制必备）**：
   - 逐项对比所有加工接触面、定位配合面及非加工面的表面粗糙度（BNIF、ANSI RMS 250/500、ISO Ra）；
   - 说明各表面对应的加工精度要求与状态定性。
5. **审核结论与纠错建议**：列出须优先修正的 Confirmed error / Confirmed omission 清单与工艺指导建议。
